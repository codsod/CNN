# `./CNN` 代码功能分析

## 1. 项目概述

本目录是一个**面向 FPGA（Xilinx Vitis HLS）的端到端整数（INT8/UINT8）量化 CNN 推理实现**。它将一份用 PyTorch（QNNPACK 后端）量化训练得到的 1D 时序 EEG 分类模型，从 PyTorch 量化模型一步步转译为 Vitis HLS C++ 代码 + 综合工程，最终用于在 KV260（Kria）类 FPGA 上跑实时推理。

任务级别上，模型完成的是 **EEG 信号 → (颜色)分类(7 类) + (色调) 角度回归(2 维 cos/sin)** 的双输出推理：见 `quant/acc0304.py` 中 `NUM_CLASSES = 7` / `CLASS_ANGLES_DEG`。

## 2. 整体网络拓扑

入口 `case/CNN_top.cpp:1-90` 用 HLS dataflow 把所有子模块串成一条流水线：

```
fp32 input
   │
   ▼
QuantInput              (fp32 → uint8, QuantStub)
   │
   ▼   tee3 一路复制成三路
ConvBrBranch0/1/2       (1D Conv + Bias + Requant + GELU LUT)
   │  (kernel = 16/32/64, out_ch = 13/13/14)
   ▼
Concat                  (三路重标定到统一量化域，按通道拼成 40 ch)
   │
   ▼
ConvSpatial             (在 IN_DIM=15 维上做 15×1 卷积 → 1×T)
   │
   ▼
SquareLUT               (x²，查表实现)
   │
   ▼
POOL                    (AvgPool, K=40, stride=20，分段累加)
   │
   ▼
LogLUT                  (log(x)，查表实现)
   │
   ▼
Split                   (一路复制成两路)
   ├─► LinearCLS        (全连接 → 7 分类 logits)
   └─► LinearHue        (全连接 → 2 维 cos/sin 回归)
```

主要超参数集中在 `src/common.h:27-52`：
- `N=1, IN_CHANNELS=1, IN_DIM=15, T=410`（实际算法 `T=410`，见 `check_ques.md:3`）
- 三个分支 kernel 为 16/32/64，输出通道 13/13/14，拼接后 `CONCAT_CHANNELS=40`
- 空间卷积 kernel `SPATIAL_KERNAL=15`
- AvgPool `K=40, S=20`，输出 `T_pool=19`
- 输出 `FC_CLS_OUT=7`、`FC_HUE_OUT=2`
- 数据类型：`X_T = ap_uint<8>`、权重 int8、bias int32、scale fp32

## 3. 各子模块功能

### 3.1 `quant_input.h` — 输入量化
对应 PyTorch `QuantStub`：`q = clamp(round(x/scale)+zp, 0, 255)`。`scale=3.542`、`zp=106`，逐元素 fp32→uint8（`src/quant_input.h:14-18`）。

### 3.2 `conv_br.h` — 三路时序 Conv + Requant + GELU
- 三路并行的 1D 因果时序卷积（`ConvBrBranch0/1/2`），输入共享，每路独立权重 / scale / zp。
- 实现细节：环形 line-buffer + `CONV_BR_C_TILE=3` 分块 + `HLS PIPELINE/UNROLL`。
- **重量化**用定点乘法实现：`prod = (acc+bias) * R_MULT`，`R_MULT = round(input_scale*weight_scale/output_scale * 2^26)`，再算术右移 26 位（`src/conv_br.h:13-34`）。
- 重量化结果直接作为 GELU LUT 的索引，得到 GELU 输出（`gelu_lut0/1/2`，每路一张表，见 `src/gelu.h`）。

### 3.3 `concat.h` — 重标定 + 通道拼接
三路的 GELU 输出原本在各自的量化域（不同 `(scale, zp)`），Concat 阶段先用 `concat_branch{0,1,2}_requant_lut` 把每路重标定到**统一的 concat 域**（`CONCAT_OUTPUT_SCALE=0.034158…, ZP=5`），再写到 `buf0/buf1/buf2`，最后按 (br0→br1→br2) × 通道 × IN_DIM × T 的顺序输出（`src/concat.h:90-135`）。

### 3.4 `conv2d.h` (`ConvSpatial`) — 空间维卷积
在 `IN_DIM=15` 维上做 15×1 卷积，把 `(40, 15, T)` 压成 `(40, 1, T)`：每个 `(oc, t)` 对所有 `ic, row` 累加 `(x - input_zp) * w`，加 bias，再做定点重量化（`src/conv2d.h:127-194`）。重量化使用 **银行家舍入 (round-half-to-even)**，与 PyTorch 量化算子的舍入对齐（`conv_spatial_requant_fixed`，`src/conv2d.h:96-111`）。

### 3.5 `square.h` — 平方激活（查表）
`out = LUT[in]`，LUT 在量化域里预先把 `x²` 算好（`src/square.h`）。

### 3.6 `pool.h` — 平均池化（K=40, S=20）
- 由于 `K=2*S`，把每段 `S=20` 的元素累加成 `seg_sum[seg]`，每个输出窗口就是相邻两段之和（`src/pool.h:36-58`）。
- 除以 40 用魔数乘 `*3277 >> 17` 实现整除，余数 `r = sum - 40q`。
- 舍入按 **银行家舍入** 实现（`2r>K` 进位、`2r<K` 截断、`==` 时向偶数舍入），见 `src/pool.h:64-74`。`check_ques.md` 已说明这是与 PyTorch trace 对齐所需。

### 3.7 `log.h` — 对数激活（查表）
逐元素 `out = log_lut[in]`，与 SquareLUT 同构（`src/log.h`）。

### 3.8 `split.h` — 一路复制为两路
分别送给分类头 `LinearCLS` 与回归头 `LinearHue`（`src/split.h`）。

### 3.9 `linear_cls.h` / `linear_hue.h` — 两个全连接头
- 每个 FC 把 `40*1*19=760` 维输入压到 7 / 2 维输出。
- 累加阶段都是整数 `(in - input_zp) * weight`，加 bias 后做 `out = clamp(round(acc * input_scale*weight_scale / output_scale) + output_zp, 0, 255)`。
- `LinearCLS` 用 `round(x+0.5)` 显式四舍五入（`src/linear_cls.h:65-69`）；`LinearHue` 是直接截断（`src/linear_hue.h:64`），`check_ques.md` 第 2 条记录了"linear 最后一步 requant 没做 round"的差异。

### 3.10 `linear.h`（旧版统一实现）
`linear.h` 用模板参数 `mode_cls` 同时支持 CLS / HUE，是早期合并版；当前 top 用的是拆分版 `linear_cls.h` / `linear_hue.h`。

### 3.11 其他源文件
- `src/conv2d.h`、`src/gelu.h`、`src/quant_input.h` 中的 scale/zp/权重/bias 都是从 `src/ref/...` 下面的 `.txt` 文件 `#include` 进来的，这些 ref 是从 `quant/acc0304.py` 量化模型导出的。
- `src/old`、`src/ref`、`src/Conv1_branch.h`、`src/conv_br.h` 等是 HLS 头文件库；`case/*.cpp` 里每个文件是该子模块的**独立 testbench / 顶层包装**（如 `CONV_BR.cpp`、`POOL.cpp`、`LINEAR.cpp`），用于单模块 csim/csynth。
- `src/utils.h`、`src/adapter.h` 是通用 helper（如 `o_stream_compare`、`clamp` 等）。

## 4. Python 工程化脚本

这一层负责"模型量化 → 生成 HLS 工程 → 跑综合 → 收集报告 → 喂给 SpinalHDL"全套自动化：

| 文件 | 作用 |
|---|---|
| `quant/acc0304.py` / `acc0304.ipynb` | 用 PyTorch QNNPACK 后端做 PTQ，导出权重/scale/zp/trace 到 `src/ref/`（fpga_bundle）。这是整条工具链的源头。 |
| `infer_conv2_input_qparams.py` | 反推 conv_spatial 真实输入 `(scale, zp)`，用真实 concat 输出 + conv2 trace 拟合，修复 `check_ques.md` 第 4 条的 conv_spatial 量化域不一致。 |
| `pre_syn_process.py` | `create_subprojects` 复制 `case/`+`src/` 到 `proj_<case>/`；`create_tcls` 用 `template.tcl` 模板生成 `run.tcl`；`run_instances` 调用 `vitis_hls -f run.tcl`。 |
| `pst_syn_process.py` | 综合后处理：解析 `vitis_hls.log` 收集 SLICE/LUT/FF/DSP/BRAM/CP 等资源；备份 verilog/log；`to_spinal` 把生成的 verilog 改写后注入 SpinalHDL 工程。 |
| `step1_simulation.py` | 第 1 步：对 `case_names` 跑 csim（默认只跑 `CNN_top`），用 Vitis HLS 2022.1。 |
| `step2_synthesis.py` | 第 2 步：对各子模块跑 csynth + syn（不含 CNN_top），输出到 `instances_tp8_syn_kv260/`。 |
| `step3_spinal_flow.py` | 第 3 步：把 SPINAL 工程涉及的若干 case（GEMM_PERMUTE、ROPE_QK_QUANT、KV_CACHE 等——是 SPINAL 那边 LLaMA 流水的产物）的 verilog 注入 SpinalHDL，与本目录的 CNN 共用同一套综合 / 转译流水。 |
| `step4_print_resource.py` | 第 4 步：调用 `print_resource_table` 打印各 case 的资源汇总。 |
| `template.tcl` | Vitis HLS 工程模板（FPGA 部件、时钟、做不做 csim/csynth/cosim/syn/impl 等开关）。 |
| `open_hls.py` | 便捷打开 Vitis HLS GUI 的脚本。 |
| `constants.py` | 路径常量；`__main__` 里另写了 LLaMA-2 解码器的存储/吞吐估算，与本网络无关。 |

## 5. `check_ques.md` 指出的当前不一致点

本目录还在持续调试中，已记录的问题（见 `check_ques.md`）：
1. `T` 实际是 410 而非 411，pool 取整需用银行家舍入。
2. `LinearHue` 最后 requant 没做 round。
3. `CONV_BR2_OUTPUT_SCALE` 与 meta 文件略有偏差；conv_br 实际算法是非对称 padding，源码近似处理。
4. `ConvSpatial` 的输入量化域历史上被硬性统一到三路最后一支，导致与真实 trace 不一致；现已用 `infer_conv2_input_qparams.py` 重新求得 `0.034158173949` 并同步修正 bias。
5. `src/old`、`new` 系列目录里实验把"重标定"放进 Concat（节省内存与时间），但单模块仿真不一致，所以两个版本并存。

## 6. 总结

`./CNN` 是这条 PyTorch QAT/PTQ → FPGA INT8 推理流水的**全部 HLS 实现 + 工程化构建脚本**。整网由 `quant_input → 3×(conv+gelu) → concat → conv_spatial → square → avgpool → log → split → 2×fc` 组成；每一步都是定点重量化版本（移位 26 位的整数乘 + 银行家舍入），所有 scale / zp / 权重 / bias 都从 `src/ref/` 的 PyTorch 量化模型导出文件 `#include` 进 C++。Python 脚本负责把每个子模块（以及完整顶层）打包成独立 Vitis HLS 工程、调起综合、收资源报告，并最终把生成的 verilog 注入到外部 SpinalHDL/KV260 工程上板。
